package com.acpulse.config;

import org.springframework.context.annotation.Configuration;

/**
 * Email Configuration
 * 
 * Spring Boot auto-configures JavaMailSender from application.properties
 * No explicit bean configuration needed - Spring Boot handles it automatically
 * based on the properties in application.properties
 */
@Configuration
public class EmailConfig {
    // Email configuration is handled by Spring Boot auto-configuration
    // based on application.properties settings
}
